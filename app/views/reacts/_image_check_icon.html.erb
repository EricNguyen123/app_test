<% if React.find_by(user_id: current_user.id, micropost_id: micropost.id) %>
  <%= render "reacts/remove_action", emotion: React.find_by(user_id: current_user.id, micropost_id: micropost.id).action %>
<% else %>
  <%= render 'reacts/image_handle_icon' %>
<% end %>
